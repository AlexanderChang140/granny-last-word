# Backend origin for Vite proxy (/api and /socket.io). Match backend PORT.
VITE_API_URL=http://localhost:3000/

# Optional: override Socket.IO URL in production (defaults to same-origin in dev, or VITE_API_URL origin when built).
# VITE_SOCKET_URL=https://api.example.com